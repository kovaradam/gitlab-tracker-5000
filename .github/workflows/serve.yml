name: Serve static filles
on: [push]

jobs:
  Serve:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd ls ${{ github.workspace }}/client
      - run: npm i
      - run: npm run serve
